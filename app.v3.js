// TERNARY OPERATOR:
// This is much easier and shorter then writing a whole if statement:

id = 100;

// If id is equal to 100
// ? THEN log CORRECT
// : ELSE log INCORRECT
console.log(id === 100 ? 'CORRECT' : 'INCORRECT');